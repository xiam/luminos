# This is a YAML file.
# Please use two-space tabs instead of the \t character.

server:
  bind: 127.0.0.1     # Address to bind to.
  port: 9000          # Port for listening to.
  type: fastcgi       # Types are "fastcgi" for a FastCGI server and "standalone" for a simple HTTP server.

hosts:
  default: "./default"
# beta.example.org: /home/jhondoe/projects/beta.example.org

